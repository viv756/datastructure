function counter(strs) {

  let pref = strs[0]
  let preLen = pref.length 

  for (let i = 1; i < strs.length; i++){
    let s = strs[i] 

    while (pref !== s.substring(0, preLen)) {
      preLen--

      pref = 
    }
  }

}